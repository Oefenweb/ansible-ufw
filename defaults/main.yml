---
# defaults file for ufw
uwf_default_policy: deny
uwf_logging: 'off'
uwf_rules:
  - rule: allow
    interface: eth0
    to_port: 22
    protocol: tcp

  - rule: allow
    interface: eth0
    to_port: 23
    protocol: udp
